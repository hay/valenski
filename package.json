{
  "name": "valenski",
  "description": "A lightweight set of SASS mixins, classes, resets and values that simplify your workflow.",
  "version": "1.3.0",
  "keywords": [
    "css",
    "scss",
    "sass",
    "mobile-first",
    "responsive",
    "front-end",
    "web"
  ],
  "scripts": {
    "build-docs": "rm -rf docs && parcel build docs-src/index.html -d docs --no-content-hash && rm docs/*.map",
    "dev-docs": "parcel docs-src/index.html"
  },
  "homepage": "http://github.com/hay/valenski",
  "devDependencies": {
    "marked": "^0.8.0",
    "parcel-bundler": "^1.12.4",
    "sass": "^1.26.3",
    "showdown": "^1.9.1"
  }
}
