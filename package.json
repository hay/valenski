{
  "name": "valenski",
  "description": "A lightweight set of SASS mixins, classes, resets and values that simplify your workflow.",
  "version": "1.5.0",
  "keywords": [
    "css",
    "scss",
    "sass",
    "mobile-first",
    "responsive",
    "front-end",
    "web"
  ],
  "scripts": {
    "build-docs": "rm -rf docs && parcel build docs-src/index.html --dist-dir docs --no-content-hash --public-url '.' && rm docs/*.map",
    "dev-docs": "parcel docs-src/index.html",
    "test": "npx sass test.scss /dev/null --verbose --no-source-map"
  },
  "homepage": "https://hay.github.io/valenski/",
  "devDependencies": {
    "@parcel/transformer-inline-string": "^2.3.2",
    "@parcel/transformer-sass": "^2.3.2",
    "sass": "^1.49.8"
  },
  "dependencies": {
    "marked": "^4.0.12",
    "parcel": "^2.3.2"
  }
}
